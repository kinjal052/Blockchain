@model IEnumerable<MVCWebApplication.Models.studentDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="AddStudent">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Standard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Standard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email)
            </td>
                @* <td>
                <td class="text-center">
                    <a asp-action="AddOrEdit" class="btn btn-outline-primary" asp-route-id="@item.Id">Edit</a>
                </td>
                <td class="text-center">
                    <a asp-action="Details" class="btn btn-outline-info" asp-route-id="@item.Id">Details</a>
                </td>
                <td class="text-center">
                    <a asp-action="DeleteStudent" class="btn btn-outline-danger" asp-route-id="@item.Id">Delete</a>
                </td> *@
                <td>
                    @Html.ActionLink("Edit", "UpdateStudentDetails", new { id = item.Id }) |
                    @Html.ActionLink("Details", "DetailsByID", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "DeleteStudent", new { id = item.Id })

                </td>
               

                
        </tr>
}
    </tbody>
</table>
